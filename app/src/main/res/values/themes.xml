<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidHomeworks" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="CustomButton" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">#FF6200EE</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:radius">150dp</item>
    </style>

    <style name="CustomButton2" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">#6200EE</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:radius">8dp</item>
    </style>

    <style name="Theme.AndroidHomeworks" parent="Base.Theme.AndroidHomeworks" />
</resources>